{
  "name": "futurecontext-edge-extension",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Local-first AI asset library for Edge and Chrome",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "node --check popup.js && node --check store.js && node --check test/store.test.mjs"
  }
}
