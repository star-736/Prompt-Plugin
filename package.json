{
  "name": "prompt-vault-edge-extension",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Offline password-protected prompt vault for Edge and Chrome",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "node --check popup.js && node --check vault.js && node --check test/vault.test.mjs"
  }
}
