# FutureContext

FutureContext 是面向未来 AI 使用的个人本地资产库：用户把可复用的 Skill、通用 Prompt 与 AIGC Prompt 收集、组织并在需要时取用。

## Language

**FutureContext**:
本产品的名称，强调为未来的 AI 创作和工作预先积累可复用资产。
_Avoid_: Prompt Vault, FuturePrompt

**AI 资产**:
用户保存并复用的 AI 工作材料，涵盖 Skill、通用 Prompt 和 AIGC Prompt。它描述内容类别，不描述是否属于私密库。
_Avoid_: 资料、笔记

**私密库**:
仅在用户进入私密库时输入隐私锁密码后才能读取的 AIGC Prompt 存储区域。它是一个可见性边界，而不是 AI 资产类型；一期不设一级分类，仅提供搜索。隐私锁可恢复，重设密码不会删除其中内容。私密库的解锁仅在当前弹窗会话有效，关闭弹窗后立即恢复锁定。它防止他人随手打开 FutureContext 查看内容，不宣称抵御浏览器配置文件或开发者工具访问。
_Avoid_: 第三类资产

**可恢复的隐私锁**:
FutureContext 对私密库提供的本地访问锁。首次进入私密库时，用户设置至少 6 位的密码并再次确认；不强制大小写或特殊字符。用户忘记密码时可从同一入口重设密码且保留内容。重设前会说明“重设后可直接进入私密库并设置新密码”，确认后不要求旧密码。它适用于防止他人直接打开扩展查看资料的场景，不是不可恢复的强加密保险箱；能操作浏览器配置文件的人也能重设后查看私密库。
_Avoid_: 不可恢复加密、强加密保险箱

**弹窗会话**:
从用户点击 Edge 工具栏中的 FutureContext 图标，到该弹窗关闭为止的一段使用时间。普通库始终可直接浏览；进入私密库时需在本会话输入密码，关闭弹窗后私密库即重新锁定。
_Avoid_: 长期登录态、后台常驻解锁

**库优先创建**:
用户先进入目标库、再创建 AI 资产的交互规则。创建私密 AIGC Prompt 必须从已解锁的私密库发起，而不是在普通的新建表单中勾选“私密”。普通 AIGC Prompt 可由用户明确操作移入私密库，确认保存成功后再删除普通库原件。反向迁移也允许，但必须二次确认它会在普通库直接可见；先成功写入普通库、再删除私密原件。
_Avoid_: 创建时选择是否私密

**自由 Prompt**:
一期的 AIGC Prompt 形态：以一个自由文本“内容”字段为主体，标题可选，分类可选；不预置示例、不强制标签，也不拆分正向提示词、负向提示词或模型参数。编辑页可复制内容本身。用户可在普通 AIGC 库中明确移入私密库，也可在已解锁私密库中直接新建。列表中没有标题时，显示内容第一行前约 32 个字作为截断摘要，原内容不被改写。
_Avoid_: 参数化 Prompt

**通用 Prompt**:
可反复使用的泛用文本提示词。编辑页只包含必填的标题、必填的多行内容和分类，不含标签、示例、模型参数或网页来源。用户保存后更新本地条目；列表行和编辑页均可通过明确的“复制”按钮复制内容本身，不带标题或分类。
_Avoid_: 参数化 Prompt、自动注入

**编辑草稿**:
用户在任一资产编辑页输入但尚未点击“保存”的本地临时内容。编辑草稿会自动保留，因此 Edge 弹窗关闭或用户离开网页后重新打开时仍可继续编辑；只有点击“保存”才创建或更新正式 AI 资产。用户在编辑页点返回且存在未保存更改时，显示“放弃未保存的更改”，操作为“继续编辑”与“放弃草稿”；直接关闭弹窗则不弹框，保留草稿。
_Avoid_: 关闭弹窗即丢失输入、输入即发布

**普通库**:
当前 Edge 浏览器配置文件中的本地明文存储区域，用于 Skill、通用 Prompt 和非私密 AIGC Prompt。它无需账号、登录或密码即可浏览；拥有该浏览器配置文件访问权的人同样可读取其中内容。
_Avoid_: 云端库、受保护库

**一级分类**:
通用 Prompt、Skill 与普通 AIGC 各自独立维护的单层扁平分类。用户可创建、重命名和删除分类；新内容默认进入“未分类”。当用户在某一分类筛选下新建条目时，新条目会预选该分类；编辑页也可直接新建分类，创建后自动选中且保留当前草稿。删除分类不会删除其中的 AI 资产，而是将它们统一移入“未分类”；若分类含有内容，确认框会展示受影响的条目数量。分类管理在弹窗内的独立子页面进行，列出分类名称和条目数，支持新建、重命名、删除，不叠加小弹窗。私密 AIGC 不设分类。FutureContext 一期不会支持嵌套子分类或无限文件夹层级。
_Avoid_: 子分类树、无限文件夹

**工具栏入口**:
FutureContext 的一期完整工作区：用户点击 Edge 工具栏中的扩展图标后打开，在该弹窗内完成编辑、一级分类、复制、删除、搜索和私密库解锁。首次使用默认打开 `通用 Prompt`；此后记住上一次停留的普通页签。顶部以 `通用 Prompt`、`Skill`、`AIGC Prompt` 三个可切换的平级页签组织内容；切换页签会同时切换列表、搜索范围、分类筛选与新建资产类型。搜索只作用于当前页签和当前库，不跨资产类型或普通/私密边界；仅在用户主动进入并解锁私密库后才搜索私密内容。每条列表行暂定仅展示标题、一行内容摘要、分类与复制按钮，默认按最近保存或编辑从新到旧排列；一期不提供手动拖拽或其他排序方式，更新时间仅在编辑页显示。进入 AIGC Prompt 后，再切换 `普通` 与需要解锁的 `私密库`；私密库绝不自动进入，解锁后不显示分类筛选。通用 Prompt、Skill 与普通 AIGC 的分类筛选默认以收起的一行 `全部分类 ▾` 呈现，点击展开后可选分类、切回全部或进入分类管理。设置页一期仅包含隐私锁状态与重设、导出全部数据和导入备份。产品不采用浏览器侧边栏或独立管理页。
_Avoid_: 浏览器侧边栏、独立管理页

**浅色资料库视觉**:
FutureContext 一期采用暖白背景、极浅灰层次与低饱和雾蓝强调色的浅色界面。私密库只使用极浅薰衣草色、锁图标和文字区分，不使用强警告色；整体参考 Linear 的克制清晰与 Raindrop 的轻盈资料库感。
_Avoid_: 深色蓝黑、强警告色、大面积高饱和色

**空状态**:
当前页签没有已保存条目时的极简页面，只显示“暂无通用 Prompt”“暂无 Skill”或“暂无 AIGC Prompt”与相应的新建按钮；不预置示例资产或长说明。
_Avoid_: 示例 Prompt、教学长文

**私密库状态图标**:
私密库在界面中使用 16px 细线条 SVG 锁图标与极浅薰衣草色文字标识，而不是 Emoji、盾牌或强警告符号。它只说明当前区域受隐私锁保护，不充当产品 Logo。
_Avoid_: 🔒 Emoji、盾牌、警告标志

**单文件 Skill**:
仅包含一个带 YAML frontmatter 的 `SKILL.md` 文件的 Skill。它是 FutureContext 第一期唯一支持的 Skill 包形态，主要用于用户自行整理的 SOP；不包含脚本、模板、参考文件或 ZIP 归档。
_Avoid_: Skill 包、多文件 Skill

**包式 Skill**:
由 `SKILL.md` 与脚本、模板、参考资料或其他资源共同构成的完整 Skill。它属于第二期范围；一期不会截取主文件来伪装成完整导入。
_Avoid_: 单文件 Skill

**原始 Markdown 编辑**:
一期创建和修改单文件 Skill 的唯一主流程：用户直接粘贴或编辑 AI 生成的完整 `SKILL.md`。FutureContext 从 YAML 的 `name` 与 `description` 读取列表标题和摘要，并在保存时校验这两个必要字段、给出错误提示；不通过额外标题或描述表单重写 Skill 的内容。
_Avoid_: 表单式 Skill 生成器

**C 字母 Logo**:
FutureContext 的主 Logo 以圆角、开口向右的几何字母 C 为核心构建，开口暗示正在展开的 Context / Prompt。它必须在 Edge 的 16px 工具栏尺寸仍可辨认，优先使用简洁 SVG 几何形状，而非位图插画；先在实施时交付一版效果，再按用户反馈调整。
_Avoid_: 锁头、通用 AI 星芒、字母 F

**显式复制**:
所有 AI 资产都由用户通过明确的“复制”按钮把文字写入剪贴板；FutureContext 不会在选中条目时自动填入网页输入框。私密库的复制与普通库一样，在明确点击后直接复制，仅显示“已复制到剪贴板”的轻提示，不追加确认弹窗。Skill 的复制内容为完整、未经表单改写的 `SKILL.md` 文字，不生成下载文件，也不支持文件上传。
_Avoid_: 自动注入、选中即复制、文件上传

**二期 LLM 整理**:
FutureContext 二期可引入用户选择的 LLM，以内容为依据生成标题、归类或标签建议；一期不依赖 LLM，亦不向网络发送 AI 资产内容。
_Avoid_: 一期自动分类、后台上传内容

**永久删除**:
一期删除 AI 资产时，FutureContext 在确认框中显示目标标题并要求二次确认；确认后立即永久删除，不设置回收站。私密库中的已删除内容不会被转存到普通库。
_Avoid_: 一期回收站、无确认删除

**确认文案**:
FutureContext 的确认弹窗标题使用明确陈述或操作短语，不使用问号。危险操作使用具体动词按钮，例如“永久删除”或“移出私密库”。AIGC 的移入与移出操作只出现在编辑页底部，和永久删除同属次要管理操作，不放入列表行。移入私密库的正文为““{标题}”将从普通 AIGC Prompt 移入私密库。移入成功后，普通库中的原件将被删除。”，按钮为“取消 / 移入私密库”；移出私密库的正文为““{标题}”将转为普通 AIGC Prompt，可在未解锁状态下直接查看。移出成功后，私密库中的原件将被删除。”，按钮为“取消 / 移出私密库”。
_Avoid_: 带问号的确认标题、含糊的“确定”按钮

**一期本地备份**:
设置页提供唯一的“导出全部数据”与导入入口。导出为一个普通 JSON 文件，包含普通库和私密库的内容，不另设备份密码或加密格式。若私密库未解锁，导出操作显示“解锁私密库后才能导出全部数据”并提供“前往解锁”；解锁成功后自动返回设置页继续导出。导出前明确提示备份文件包含私密内容、需由用户自行妥善保存。导入不要求解锁：私密内容会直接写入私密库、但不展示，之后由当前设备的隐私锁保护。导入是非破坏式合并：不覆盖、不删除现有内容，跳过完全相同的条目，并在结束后展示导入与跳过的数量。真正加密的备份文件属于第二期。
_Avoid_: 一期备份密码、两份备份文件、无提示导出私密内容
