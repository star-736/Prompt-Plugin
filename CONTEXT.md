# FutureContext

FutureContext 是面向未来 AI 使用的个人本地资产库：用户把可复用的 Skill、通用 Prompt、AIGC Prompt 与终端指令收集、组织并在需要时取用。

## Language

**FutureContext**:
本产品的名称，强调为未来的 AI 创作和工作预先积累可复用资产。
_Avoid_: Prompt Vault, FuturePrompt

**AI 资产**:
用户保存并复用的工作材料，涵盖 Skill、通用 Prompt、AIGC Prompt 和终端指令。它描述内容类别，不描述是否属于私密库。
_Avoid_: 资料、笔记

**私密库**:
仅在用户进入私密库时输入隐私锁密码后才能读取的 AIGC Prompt 存储区域。它是一个可见性边界，而不是 AI 资产类型；一期不设一级分类，仅提供搜索。隐私锁可恢复，重设密码不会删除其中内容。私密库的解锁仅在当前弹窗会话有效，关闭弹窗后立即恢复锁定。它防止他人随手打开 FutureContext 查看内容，不宣称抵御浏览器配置文件或开发者工具访问。
_Avoid_: 第三类资产

**可恢复的隐私锁**:
FutureContext 对私密库提供的本地访问锁。首次进入私密库时，用户设置至少 6 位的密码并再次确认；不强制大小写或特殊字符。用户忘记密码时可从同一入口重设密码且保留内容。重设前会说明“重设后可直接进入私密库并设置新密码”，确认后不要求旧密码。它适用于防止他人直接打开扩展查看资料的场景，不是不可恢复的强加密保险箱；能操作浏览器配置文件的人也能重设后查看私密库。
_Avoid_: 不可恢复加密、强加密保险箱

**弹窗会话**:
从用户点击 Edge 工具栏中的 FutureContext 图标，到该弹窗关闭为止的一段使用时间。普通库始终可直接浏览；进入私密库时需在本会话输入密码，关闭弹窗后私密库即重新锁定。
_Avoid_: 长期登录态、后台常驻解锁

**库优先创建**:
用户先进入目标库、再创建 AI 资产的交互规则。创建私密 AIGC Prompt 必须从已解锁的私密库发起，而不是在普通的新建表单中勾选“私密”。普通 AIGC Prompt 可由用户明确操作移入私密库，确认保存成功后再删除普通库原件。反向迁移也允许，但必须二次确认它会在普通库直接可见；先成功写入普通库、再删除私密原件。
_Avoid_: 创建时选择是否私密

**自由 Prompt**:
一期的 AIGC Prompt 形态：以一个自由文本“内容”字段为主体，标题可选，分类可选；不预置示例、不强制标签，也不拆分正向提示词、负向提示词或模型参数。二期起普通与私密 AIGC 都取消标题与分类字段，AI 也不处理 AIGC；列表以 Prompt 正文为卡片主体，按卡片宽度显示前两行并省略，原内容不被改写。为兼容已有备份，一期普通 AIGC 的分类关联保留在数据中，但不在二期界面显示或参与筛选。编辑页可复制内容本身。用户可在普通 AIGC 库中明确移入私密库，也可在已解锁私密库中直接新建。
_Avoid_: 参数化 Prompt

**通用 Prompt**:
可反复使用的泛用文本提示词。二期起编辑页包含可选标题、多行内容和分类，不含标签、示例、模型参数或网页来源；未填标题时先显示正文摘要，后台 AI 可在保存后补写简短标题。用户手动输入或修改标题后，该标题成为人工决定，后台不会覆盖。用户保存后更新本地条目；列表行和编辑页均可通过明确的“复制”按钮复制内容本身，不带标题或分类。
_Avoid_: 参数化 Prompt、自动注入

**终端指令**:
用户保存并复用的终端、CLI 或 AI Agent 命令。命令正文本身即标识，不设标题；只存在于普通库，并有独立的一级分类。它不是 Prompt，不进入私密库，不发送给后台 AI，默认也不出现在取用面板。
_Avoid_: 终端命令、脚本资产、第四类 Prompt

**编辑草稿**:
用户在任一资产编辑页输入但尚未点击“保存”的本地临时内容。编辑草稿会自动保留，因此 Edge 弹窗关闭或用户离开网页后重新打开时仍可继续编辑；只有点击“保存”才创建或更新正式 AI 资产。草稿永不发送给二期后台 AI。用户在编辑页点返回且存在未保存更改时，显示“放弃未保存的更改”，操作为“继续编辑”与“放弃草稿”；直接关闭弹窗则不弹框，保留草稿。
_Avoid_: 关闭弹窗即丢失输入、输入即发布

**普通库**:
当前 Edge 浏览器配置文件中的本地明文存储区域，用于 Skill、通用 Prompt、终端指令和非私密 AIGC Prompt。它无需账号、登录或密码即可浏览；拥有该浏览器配置文件访问权的人同样可读取其中内容。
_Avoid_: 云端库、受保护库

**一级分类**:
通用 Prompt、Skill 与终端指令各自独立维护的单层扁平分类。用户可创建、重命名和删除分类；新内容默认进入“未分类”。当用户在某一分类筛选下新建条目时，新条目会预选该分类；编辑页也可直接新建分类，创建后自动选中且保留当前草稿。删除分类不会删除其中的 AI 资产，而是将它们统一移入“未分类”；若分类含有内容，确认框会展示受影响的条目数量。分类管理在弹窗内的独立子页面进行，列出分类名称和条目数，支持新建、重命名、删除，不叠加小弹窗。二期后台 AI 可为未手动分类的通用 Prompt 与 Skill 自动归入已有分类；累计到阈值的不匹配内容可自动形成新分类。AI 不自动重命名、合并或拆分已有分类，发现此类优化机会时只提供方案，由用户应用。终端指令的分类只由用户维护。AIGC 不设分类。FutureContext 不会支持嵌套子分类或无限文件夹层级。
_Avoid_: 子分类树、无限文件夹

**工具栏入口**:
FutureContext 的完整工作区：用户点击 Edge 工具栏中的扩展图标后打开，在该弹窗内完成编辑、一级分类、复制、删除、搜索、私密库解锁和后台 AI 设置。首次使用默认打开 `通用 Prompt`；此后记住上一次停留的普通页签。顶部以 `通用 Prompt`、`Skill`、`AIGC Prompt`、`终端指令` 四个可切换的平级页签组织内容；切换页签会同时切换列表、搜索范围、分类筛选与新建资产类型。四个页签保持同一字号与间距，不折行、不缩小；`终端指令` 排在最后。工具栏入口内的搜索只作用于当前页签和当前库，不跨资产类型或普通/私密边界；仅在用户主动进入并解锁私密库后才搜索私密内容（取用面板的跨类型搜索是另一回事，见“取用面板”）。通用 Prompt 和 Skill 列表显示标题、内容摘要、分类与复制按钮；AIGC 卡片显示正文前两行和复制按钮；终端指令卡片以等宽命令正文为主体并显示分类。进入 AIGC Prompt 后，再切换 `普通` 与需要解锁的 `私密库`；私密库绝不自动进入，解锁后不显示分类筛选。通用 Prompt、Skill 与终端指令的分类筛选默认以收起的一行 `全部分类 ▾` 呈现，点击展开后可选分类、切回全部或进入分类管理。设置页包含隐私锁、Provider、后台 AI 阈值与诊断、分类建议、启用站点、取用次数概览、导入和导出。产品不采用浏览器侧边栏或独立管理页；取用面板只是覆盖在网页输入框上的取用入口，不承担管理职责。
_Avoid_: 浏览器侧边栏、独立管理页

**浅色资料库视觉**:
FutureContext 一期采用暖白背景、极浅灰层次与低饱和雾蓝强调色的浅色界面。私密库只使用极浅薰衣草色、锁图标和文字区分，不使用强警告色。终端指令页签的激活态使用低饱和绿色，仅作用于该页签文字与下划线，以标明它不是 Prompt 资产；其它页签、按钮与列表仍用雾蓝。整体参考 Linear 的克制清晰与 Raindrop 的轻盈资料库感。
_Avoid_: 深色蓝黑、强警告色、大面积高饱和色

**空状态**:
当前页签没有已保存条目时的极简页面，只显示“暂无通用 Prompt”“暂无 Skill”“暂无 AIGC Prompt”或“暂无终端指令”与相应的新建按钮；不预置示例资产或长说明。
_Avoid_: 示例 Prompt、教学长文

**私密库状态图标**:
私密库在界面中使用 16px 细线条 SVG 锁图标与极浅薰衣草色文字标识，而不是 Emoji、盾牌或强警告符号。它只说明当前区域受隐私锁保护，不充当产品 Logo。
_Avoid_: 🔒 Emoji、盾牌、警告标志

**单文件 Skill**:
仅包含一个带 YAML frontmatter 的 `SKILL.md` 文件的 Skill。它是 FutureContext 第一期唯一支持的 Skill 包形态，主要用于用户自行整理的 SOP；不包含脚本、模板、参考文件或 ZIP 归档。
_Avoid_: Skill 包、多文件 Skill

**包式 Skill**:
由 `SKILL.md` 与脚本、模板、参考资料或其他资源共同构成的完整 Skill。它属于第二期范围；一期不会截取主文件来伪装成完整导入。
_Avoid_: 单文件 Skill

**原始 Markdown 编辑**:
一期创建和修改单文件 Skill 的唯一主流程：用户直接粘贴或编辑 AI 生成的完整 `SKILL.md`。FutureContext 从 YAML 的 `name` 与 `description` 读取列表标题和摘要，并在保存时校验这两个必要字段、给出错误提示；不通过额外标题或描述表单重写 Skill 的内容。
_Avoid_: 表单式 Skill 生成器

**C 字母 Logo**:
FutureContext 的主 Logo 以圆角、开口向右的几何字母 C 为核心构建，开口暗示正在展开的 Context / Prompt。它必须在 Edge 的 16px 工具栏尺寸仍可辨认，优先使用简洁 SVG 几何形状，而非位图插画；先在实施时交付一版效果，再按用户反馈调整。
_Avoid_: 锁头、通用 AI 星芒、字母 F

**显式复制**:
所有 AI 资产都由用户通过明确的“复制”按钮把文字写入剪贴板；FutureContext 不会在选中条目时自动填入网页输入框。私密库的复制与普通库一样，在明确点击后直接复制，仅显示“已复制到剪贴板”的轻提示，不追加确认弹窗。Skill 的复制内容为完整、未经表单改写的 `SKILL.md` 文字，不生成下载文件，也不支持文件上传。三期起，就地取用是与显式复制并列的另一条由用户明确触发的取用路径；二者都会计入取用次数。
_Avoid_: 自动注入、选中即复制、文件上传

**就地取用**:
用户在启用站点的网页输入框中输入触发符 `//` 或按下快捷键，从取用面板选中一条普通库资产并按 Enter 确认后，FutureContext 把该资产的内容写入当前输入框的行为。写入前必须有用户的明确确认动作；它区别于自动注入，后者指未经确认就填入或改写网页内容。就地取用只写入用户正在输入的那个输入框，不读取页面其他内容，不替用户发送消息。
_Avoid_: 自动注入、自动填充、一键发送

**取用面板**:
就地取用时覆盖在网页输入框上方的轻量搜索面板。它按当前页面 URL 过滤资产类型：聊天页只搜通用 Prompt 与 Skill，不含 AIGC 与终端指令；`grok.com/imagine` 等绘画页仅搜普通 AIGC Prompt。按置顶、匹配度与取用次数排序，用 ↑↓ 选择、Enter 插入、Esc 关闭。私密库内容永不出现在取用面板中；面板不提供编辑、删除或分类管理，那些仍属于工具栏入口。
_Avoid_: 页内管理页、浮动侧边栏、迷你弹窗

**启用站点**:
用户在工具栏入口为某个网站明确开启就地取用后，该网站成为启用站点。弹窗顶部仅对常见 AI 站点提示启用；其它站点在设置里手动启用。FutureContext 只在启用站点加载取用面板，且仅读取当前获得焦点的输入框中光标附近的文字以识别触发符。用户可随时停用；未启用的网站上 FutureContext 不加载任何页面代码。
_Avoid_: 全站默认注入、自动识别 AI 网站

**就地保存**:
用户在任意网页选中文字后，通过右键菜单“保存到 FutureContext”把选中文字直接保存为一条无标题、未分类的通用 Prompt 的行为。它不经过编辑页，保存后由后台 AI（若已开启）补标题与归类。就地保存只读取用户主动选中的那段文字，不采集页面其他内容。
_Avoid_: 自动采集、剪藏网页、保存整段对话

**取用次数**:
一条 AI 资产被显式复制或就地取用的累计次数与最近一次取用时间。它只保存在本地，用于常用排序和取用面板排序；FutureContext 不上传任何使用数据。取用次数在导出备份中随资产一起保留。
_Avoid_: 使用统计上报、遥测

**常用排序**:
通用 Prompt、Skill、普通 AIGC 与终端指令列表可在“最近编辑”“最近取用”“最常取用”三种顺序中切换，选择按页签记住。默认为最近编辑，与一期保持一致。置顶条目始终位于任一排序的顶部。
_Avoid_: 手动拖拽排序、智能排序

**置顶**:
用户为普通库中的一条 AI 资产手动标记的固定优先级。置顶条目在列表与取用面板中始终排在最前，置顶之间按当前排序规则排列。置顶是人工决定，后台 AI 不会设置或取消它。私密库不提供置顶。
_Avoid_: 收藏夹、星标分组

**二期 LLM 整理**:
FutureContext 二期由用户自行选择云端 LLM Provider 并配置自己的凭据，可保存多个 Provider 配置但后台同一时刻只使用一个当前 Provider。开启后台整理不会回扫旧库：只有之后新保存、或正文实质编辑后再次保存的通用 Prompt 与 Skill 才自动进入队列；旧内容只在用户明确执行“整理现有内容”后处理。扩展可为通用 Prompt 生成标题，并为通用 Prompt 与 Skill 归类；每个资产类型的库累积到用户设置的未分类条目数时，可自动建立首批分类。首批分类建立后，用户可在设置中配置分类重整所需的新增/编辑条数与最短间隔天数，不设置独立的最小库规模。用户手动填写的标题或手动选择的分类始终优先且不会被后台结果覆盖，且已手动分类的条目不被 AI 移动。AIGC Prompt 与终端指令永不由后台发送给 Provider。产品不引入账号或云同步。
_Avoid_: 一期自动分类、私密库后台上传、平台内置账号

**AI 整理记录**:
一次后台 AI 对一组条目所做的标题、归类或创建分类操作及其变更映射。常规自动整理不打断用户；涉及重命名、合并或拆分已有分类的方案必须先由用户应用，应用后形成可追溯记录。记录的调整与一键撤销属于后续增强，不能影响用户用现有编辑和分类管理功能修正 AI 结果。
_Avoid_: 前置审批每条内容、不可追溯的结构变更、整库回滚

**AI 密钥保险盒**:
用户选择保存 Provider API Key 时，以 FutureContext 的私密库密码加密该凭据。每个 Edge 浏览器会话中，用户首次需要 AI 时输入一次该密码；随后后台整理可在本会话无感使用密钥。重设私密库密码会使旧密钥无法解密，因此只清空 Provider 配置，不影响任何 AI 资产或私密库内容。
_Avoid_: 每次调用输入密码、明文长期保存 API Key、第二套独立密码

**Provider 配置**:
用户保存的一组云端模型设置，包含 Provider 类型、显示名称、用户手填的 Model ID 与必要的端点信息；OpenAI、OpenCode Go、DeepSeek、OpenRouter 是预设类型，自定义 Provider 采用 OpenAI 兼容格式并要求 Base URL。界面为预设 Provider 提供 Model ID 示例和“测试连接”，不自动拉取模型目录。FutureContext 可保存多个配置，但只有用户明确选定的一组可被后台整理使用；API Key 不以明文保存。
_Avoid_: 平台内置账号、多个后台 Provider 并行运行、任意协议自定义接口、自动模型发现

**GitHub Skill 收集**:
二期用户在公开 GitHub 仓库的具体 `SKILL.md` 文件页面明确发起的 Skill 保存操作。FutureContext 保存该 `SKILL.md` 所在目录的全部内容（包括资源与脚本），并记录仓库 URL、目录路径与当前 Commit；单个 Skill 上限为 10 MB、其中单个文件上限为 5 MB，任一超限则整体拒绝保存并列出超限文件。已保存的 Skill 可在详情页手动“检查 GitHub 更新”，以该仓库默认分支的同一路径比较当前 Commit；发现变化后只提供直接更新，更新保留原条目的分类和本地编辑信息，并替换目录内容。相同仓库、路径与 Commit 的重复收集只提示“已是当前保存版本”。详情页以只读文件树呈现包内容；文本文件可查看，脚本标明“仅保存，不执行”，二进制资源只显示名称与大小。它不会执行脚本、上传内容到 Claude/Grok 等网页，或替用户操作任何第三方平台；跨平台上传和使用工作流留待真实需求出现后设计。
_Avoid_: 私有仓库登录、本地 ZIP 作为主入口、仓库首页全量扫描、自动抓取网页、脚本执行、二期网页上传、自动更新

**投递到 Agent**:
用户把一条已保存的普通库 Skill 明确写入某个 Agent 的本地 skills 目录，使该 Agent 可以发现它。收集本身不是投递；未投递的 Skill 对任何 Agent 不可见。
_Avoid_: 同步到 Agent、自动注入、安装 Skill、导出到 Agent

**撤回投递**:
用户从已绑定的 Agent skills 目录中删除 FutureContext 为某条 Skill 写下的副本。只删除带投递标记且属于该条目的目录；库内收藏保留。
_Avoid_: 卸载 Skill、回滚库、同步删除

**Agent 目录绑定**:
用户为某个 Agent 选择的本机 skills 文件夹。绑定只记住写入位置，不把库内 Skill 写进去。
_Avoid_: 自动发现 Agent 目录、全库同步

**永久删除**:
一期删除 AI 资产时，FutureContext 在确认框中显示目标标题并要求二次确认；确认后立即永久删除，不设置回收站。私密库中的已删除内容不会被转存到普通库。
_Avoid_: 一期回收站、无确认删除

**确认文案**:
FutureContext 的确认弹窗标题使用明确陈述或操作短语，不使用问号。危险操作使用具体动词按钮，例如“永久删除”或“移出私密库”。AIGC 的移入与移出操作只出现在编辑页底部，和永久删除同属次要管理操作，不放入列表行。移入私密库的正文为““{标题}”将从普通 AIGC Prompt 移入私密库。移入成功后，普通库中的原件将被删除。”，按钮为“取消 / 移入私密库”；移出私密库的正文为““{标题}”将转为普通 AIGC Prompt，可在未解锁状态下直接查看。移出成功后，私密库中的原件将被删除。”，按钮为“取消 / 移出私密库”。
_Avoid_: 带问号的确认标题、含糊的“确定”按钮

**一期本地备份**:
设置页提供唯一的“导出全部数据”与导入入口。导出为一个普通 JSON 文件，包含普通库和私密库的内容，不另设备份密码或加密格式。若私密库未解锁，导出操作显示“解锁私密库后才能导出全部数据”并提供“前往解锁”；解锁成功后自动返回设置页继续导出。导出前明确提示备份文件包含私密内容、需由用户自行妥善保存。导入不要求解锁：私密内容会直接写入私密库、但不展示，之后由当前设备的隐私锁保护。导入是非破坏式合并：不覆盖、不删除现有内容，跳过完全相同的条目，并在结束后展示导入与跳过的数量。真正加密的备份文件属于第二期。
_Avoid_: 一期备份密码、两份备份文件、无提示导出私密内容
