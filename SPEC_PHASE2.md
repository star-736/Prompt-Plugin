# FutureContext 二期产品规格

## 目标

在保持“本地优先、无账号、无云同步、全程弹窗操作”的前提下，加入两项能力：

1. 用户自配云端 LLM 的后台整理，用于通用 Prompt 和 Skill；
2. 从公开 GitHub 的具体 `SKILL.md` 页面收集完整包式 Skill，只保存，不执行。

## 范围与边界

- 仍只使用 Edge 工具栏弹窗，不新增 Side Panel 或独立管理页。
- 不读取、自动采集或自动填充第三方 AI 网页；不上传 Skill 到 Claude、Grok、Kimi 等平台。
- 不引入登录、账号、云同步或 FutureContext 自有后端。
- AIGC Prompt（普通和私密）与终端指令永不发送给 LLM，也不做标题、分类或标签整理。
- 草稿永不发送；只有保存的新条目，或正文发生实质修改后再次保存的既有条目，才可能进入后台队列。

## AIGC 与通用 Prompt 的呈现

| 资产 | 二期编辑字段 | 卡片呈现 | LLM 处理 |
| --- | --- | --- | --- |
| 通用 Prompt | 可选标题、正文、分类 | 有标题则显示标题；无标题先显示正文摘要 | 可补短标题、归类 |
| Skill | 原始 `SKILL.md` 或完整 GitHub 包、分类 | YAML `name` 和 `description` | 可归类，不改写 YAML |
| AIGC Prompt（普通/私密） | 仅正文 | 正文前两行，超出省略 | 永不处理 |
| 终端指令 | 命令正文、分类；无标题 | 等宽命令正文前两行 | 永不处理 |

一期既有 AIGC 的标题和分类数据保留在本地与备份中，但二期界面不再显示、编辑、筛选或使用这些字段。

## 后台 AI 整理

### Provider 与密钥

- 支持 OpenAI、OpenCode Go、DeepSeek、OpenRouter，以及 OpenAI 兼容的自定义 Base URL。
- 用户可保存多个 Provider 配置，但只能明确选择一个当前 Provider。
- 用户手填 Model ID；预设 Provider 只提供示例与“测试连接”，不拉取模型列表。
- API Key 以私密库密码加密后本地保存；不以明文长期保存。
- 每次 Edge 浏览器会话首次使用后台 AI，用户输入一次私密库密码；本次浏览器会话内后台可无感运行。重启浏览器后需再次解锁。
- 重设私密库密码不删除任何资产或私密内容，但旧 API Key 不可解密，因此清空 Provider 密钥配置并要求重新填写。
- Provider 出错、密钥失效或连接失败时，不弹窗打断用户；设置页展示“后台整理暂停，检查 Provider 配置”。

### 自动整理规则

- 保存后短暂合并请求（约 10 秒）再处理，不阻塞保存。
- 用户手填或修改的通用 Prompt 标题绝不被 AI 覆盖。
- 用户手动指定分类的通用 Prompt 或 Skill，绝不被 AI 移动。
- 未手动分类的通用 Prompt 与 Skill 可被自动归入既有分类。
- 同一资产类型累计达到用户设置数量的未分类内容时，AI 可创建首批或新增分类并完成归类。默认阈值为 7 条。
- 只有用户主动执行“整理现有内容”时，历史内容才会进入处理范围。

### 既有分类结构优化

- AI 不会自动重命名、合并或拆分已有分类。
- 当初始分类已经存在、达到“新增/正文实质编辑条数”阈值且超过冷却天数时，AI 可生成结构优化建议。默认分别为 7 条和 14 天；两项均可在设置中修改。
- 建议以非阻塞卡片出现，例如把“写邮件 / 周报 / 会议纪要”建议归纳为“工作沟通”。用户可查看方案、调整后应用，或保持现有结构。
- 常规归类可以静默完成；结构性变更必须由用户应用。失败保持静默，仅在设置显示诊断状态。

## GitHub 包式 Skill 收集

### 发起与保存

- 只支持公开 GitHub 仓库。
- 用户必须位于具体 `SKILL.md` 文件页面，并主动点击收集；不会从仓库首页扫描或猜测目录。
- 保存该 `SKILL.md` 所在目录的完整递归内容，包括 `scripts/`、`references/`、`assets/` 等。
- 保存来源仓库 URL、目录路径和精确 Commit。
- 单个 Skill 总大小最多 10 MB，单个文件最多 5 MB；任何文件超限则整体拒绝保存，界面列出具体文件及其大小。
- 不执行、不解析运行脚本；不将内容上传到任何第三方 AI 平台。

### 查看与更新

- Skill 详情提供只读文件树：文本可查看；脚本附“仅保存，不执行”；二进制资源仅显示名称和大小。
- 详情页提供“检查 GitHub 更新”。它比较已保存版本与来源仓库默认分支下相同路径的版本；绝不自动检查或自动更新。
- 有更新时只提供“更新”：用最新完整目录替换已保存包，保留该条目的分类和本地编辑信息。
- 相同仓库、路径与 Commit 的再次收集不创建副本，只提示“已是当前保存版本”。

## 设置与权限

- 设置页新增“后台 AI”区域：开关、当前 Provider、Provider 管理、测试连接、整理现有内容、三个阈值和运行诊断。
- 对预设 Provider，只在用户配置该 Provider 时请求其对应 API 域名的可选主机权限。
- 对自定义 OpenAI 兼容端点，只在用户输入并确认该 Base URL 后请求该具体站点的权限；扩展不默认获得所有网站读取权限。
- GitHub 收集只为 GitHub 请求必要的可选主机权限。

## 数据与兼容性

- 保留一期数据和备份兼容性，不迁移或删除已有资产。
- 包式 Skill 资源使用本地浏览器存储保存；常规条目与包资源的备份/导入都应保持非破坏式合并语义。
- 原有单文件 Skill 继续可编辑、复制并分类；它们不被强制转换成 GitHub 包式 Skill。

## 明确延后

- GitHub 以外的 SkillHub、ZIP、本地文件导入与私有仓库。
- Skill 运行、脚本执行、Sandbox、Claude/Grok/Kimi 等网站的一键上传或拖拽。
- 自动拉取 Provider 模型目录、Provider 并行处理。
- AI 标签生成、AIGC 整理、私密 AIGC 后台整理。
- 云端同步、账号、原生系统钥匙串、完整的 AI 操作一键撤销。
