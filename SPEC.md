# FutureContext 一期产品规格

## 产品目标

FutureContext 是一个 Edge 工具栏弹窗中的本地 AI 资产库。它让用户保存、分类、编辑和复制通用 Prompt、单文件 Skill 与 AIGC Prompt；AIGC 的私密内容由可恢复的本地隐私锁遮挡。

## 一期范围

- 全部操作在 Edge 扩展弹窗内完成，不使用 Side Panel、独立管理页、账号或云同步。
- 本地保存三类资产：通用 Prompt、单文件 Skill、AIGC Prompt。
- 通用 Prompt、Skill、普通 AIGC 具备各自独立的一级分类；私密 AIGC 不分类。
- 支持搜索、编辑、显式复制、删除、AIGC 的普通/私密迁移，以及导入/导出完整备份。

## 资产与编辑规则

| 资产 | 编辑内容 | 列表标题 | 复制内容 |
| --- | --- | --- | --- |
| 通用 Prompt | 必填标题、必填多行内容、分类 | 标题 | 内容正文 |
| 单文件 Skill | 一个完整 `SKILL.md` 原始 Markdown 编辑区 | YAML `name`，摘要为 `description` | 完整原始 `SKILL.md` |
| 普通 AIGC Prompt | 可选标题、必填多行内容、分类 | 标题；无标题时取第一行前约 32 个字 | 内容正文 |
| 私密 AIGC Prompt | 可选标题、必填多行内容 | 标题；无标题时取第一行前约 32 个字 | 内容正文 |

Skill 仅支持带 YAML frontmatter 的单个 `SKILL.md`，保存时校验 `name` 与 `description`。脚本、模板、参考资料、ZIP、包式 Skill、GitHub 捕获适配均不在一期范围。

## 主界面与导航

- 弹窗采用较大的约 720 × 600 浅色工作区。
- 首次打开默认进入 `通用 Prompt`；之后记住上次的普通页签。
- 顶部页签顺序固定为：`通用 Prompt`、`Skill`、`AIGC Prompt`。
- AIGC 内再显示 `普通` 与 `私密库`。私密库永不自动进入。
- 普通页签的工具区为：当前范围搜索、`全部分类 ▾`、`+ 新建`。
- 私密库解锁后只显示当前范围搜索与 `+ 新建`，不显示分类。
- 搜索不跨资产类型或普通/私密边界；仅解锁后搜索私密内容。
- 条目按最近保存或编辑倒序排列。列表行显示标题、一行内容摘要、分类及明确的复制按钮；更新时间只在编辑页显示。
- 点击条目进入编辑页。编辑页提供返回、复制、保存、以及必要的管理操作。
- 空列表只显示“暂无通用 Prompt”“暂无 Skill”或“暂无 AIGC Prompt”与新建按钮。

## 分类与草稿

- 分类是每个普通资产区域独立维护的单层结构；新条目默认归入“未分类”。
- 在某分类筛选下新建时，自动预选该分类。
- 分类菜单可进入弹窗内的“管理分类”子页，显示名称与条目数，支持新建、重命名、删除。
- 删除分类时，所属条目移入“未分类”；若存在条目，确认框显示受影响数量。
- 编辑内容自动保留为草稿，弹窗关闭后重新打开仍可继续；“保存”才创建或更新正式条目。
- 编辑页返回且有未保存修改时，提示“放弃未保存的更改”，操作为“继续编辑 / 放弃草稿”。直接关闭弹窗不打断，保留草稿。

## 私密库

- 私密库只保存 AIGC Prompt，用于防止他人随手打开扩展查看内容，不是强加密保险箱。
- 首次进入私密库时设置至少 6 位、重复确认的密码；之后每次新打开弹窗并进入私密库时都需输入密码。
- 解锁仅在当前弹窗会话有效，关闭弹窗立即重新锁定。
- 忘记密码可在同一入口重设；说明“重设后可直接进入私密库并设置新密码”，确认后不要求旧密码且不删除内容。
- 锁定时不显示条目数量、标题、摘要或时间，只显示细线 SVG 锁图标、`私密库`、解锁按钮。
- 普通 AIGC 可从编辑页底部明确“移入私密库”；私密 AIGC 可在同一位置“移出私密库”。目标写入成功后才删除原件。

## 复制、删除与确认

- 所有复制均由明确的“复制”按钮触发，复制后仅显示“已复制到剪贴板”。不会自动填入网页输入框，也不下载文件。
- 删除确认标题为“永久删除”，包含目标标题与“将被永久删除，无法恢复”，按钮为“取消 / 永久删除”。一期无回收站。
- 移入私密库：`“{标题}”将从普通 AIGC Prompt 移入私密库。移入成功后，普通库中的原件将被删除。`
- 移出私密库：`“{标题}”将转为普通 AIGC Prompt，可在未解锁状态下直接查看。移出成功后，私密库中的原件将被删除。`
- 所有确认标题均不用问号，危险操作按钮使用具体动词。

## 备份与导入

- 设置页提供“导出全部数据”与导入入口。
- 导出一个普通 JSON 文件，包含普通库与私密库内容，不设备份密码或加密格式。
- 私密库锁定时，导出提示“解锁私密库后才能导出全部数据”，提供“前往解锁”；解锁后自动回到设置继续导出。
- 导出前说明备份文件包含私密内容，用户自行妥善保存。
- 导入不要求解锁；私密内容直接写入但不展示在私密库外。
- 导入只合并：不覆盖、不删除，跳过完全相同条目，结束时展示导入与跳过数量。

## 视觉与品牌

- 名称为 FutureContext。
- 采用暖白底、极浅灰层次、低饱和雾蓝强调色；私密区域用极浅薰衣草与细线锁图标区分。
- 参考 Linear 的克制清晰和 Raindrop 的轻盈资料库感。
- Logo 是开口向右、圆角几何的字母 C，暗示展开的 Context / Prompt；以 SVG 实现，16px Edge 工具栏尺寸可辨认。

## 明确不做

- 浏览器侧边栏、账号、登录、云同步、自动采集网页、自动注入网页输入框。
- 文件上传、ZIP、`.skill`、包式 Skill、脚本执行、图片附件。
- 标签、模型参数、正负提示词拆分、示例 Prompt、LLM 自动整理。
- 回收站、手动拖拽排序、主题切换、真正加密的备份文件。

## 二期方向

- 支持包式 Skill 及其资源。
- 用户选择的 LLM 生成标题、分类或标签建议；一期不向网络发送资产内容。
- 加密备份与其他需要额外密码的隐私能力。
