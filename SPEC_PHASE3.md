# FutureContext 三期产品规格

## 目标

一期和二期把 FutureContext 做成了一个克制、完整的本地资产库，但取用路径只有弹窗里的“复制”按钮：想起工具 → 点图标 → 切页签 → 搜索 → 复制 → 切回 AI 网页 → 粘贴。这条路径与“把 Prompt 存在笔记软件里”摩擦相当，是使用频率低的结构性原因。

三期不再增加“存”和“管”的能力，只做三件让库被真正用起来的事：

1. **就地取用**：在 AI 聊天页的输入框里输入 `//` 或按快捷键，调出取用面板，搜索并直接插入资产；
2. **就地保存**：在任意网页选中文字后右键保存为通用 Prompt；
3. **常用排序**：记录取用次数，提供最近取用 / 最常取用排序与置顶，让常用条目浮上来。

## 范围与边界

- 继续保持本地优先、无账号、无云同步、不采用侧边栏或独立管理页。
- 取用面板与右键保存是用户明确触发的动作，属于显式操作；FutureContext 仍不做自动注入、自动填充、自动发送或自动采集。
- 私密库内容永不出现在取用面板、右键菜单或任何页面代码可读的位置。私密库不参与置顶和常用排序。
- 页面代码只在启用站点加载；只读取当前焦点输入框中的文字以识别触发符和替换触发文本，不读取页面其他 DOM、不监听网络、不向任何服务器发送数据。
- 取用次数只保存在本地，不上报。
- 二期的后台 AI、GitHub Skill 收集、Provider 逻辑不改变；就地保存的条目沿用二期的入队规则。

## 就地取用

### 启用站点

- 设置页新增“就地取用”区域：总开关、启用站点列表、触发符开关、快捷键说明。
- 弹窗主界面顶部仅对常见 AI 站点提示启用就地取用；其它站点在设置里手动启用。点击后请求该域名的可选主机权限，成功后该站成为启用站点。
- 设置页提供常见 AI 站点的预设列表（ChatGPT、Claude、Gemini、DeepSeek、Kimi、Grok），一键启用；也接受用户输入任意 `https://` 域名。
- 停用某站点时同步撤销该域名的主机权限并卸载页面代码。
- 未启用的网站上不注册任何 content script。

### 触发与面板

- 在启用站点，当用户在 `textarea`、`input[type=text]` 或 `contenteditable` 元素中连续输入 `//` 时，面板出现在该输入框上方（空间不足时在下方）；继续输入的字符作为搜索词并实时过滤。
- 面板也可由快捷键唤起，默认 `Alt+Shift+F`，用户可在 `edge://extensions/shortcuts`（Chrome 为 `chrome://extensions/shortcuts`）修改。快捷键唤起时不要求已输入 `//`。
- 使用 `//` 而不是单个 `/`，避免与 ChatGPT、Claude 等站点自带的斜杠菜单冲突。用户可在设置中关闭触发符，仅保留快捷键。
- 面板列出最多 8 条结果，每条显示：类型标记（通用 / Skill / AIGC）、标题（或正文摘要）、一行内容摘要、置顶标记。
- 排序：置顶优先 → 标题匹配优先于正文匹配 → 取用次数降序 → 最近编辑降序。搜索词为空时按置顶、取用次数、最近编辑排序。
- 搜索范围按当前页面 URL 过滤类型：聊天页（含 ChatGPT / Claude / Gemini / DeepSeek / Kimi / Grok 聊天页，以及用户手动启用的其它站）只搜通用 Prompt 与 Skill，不含 AIGC 与终端指令；`grok.com/imagine`（含子路径）仅搜普通 AIGC Prompt。私密 AIGC 与终端指令永不出现。搜索匹配标题、正文与分类名，忽略大小写和多余空白。
- 匹配必须看 path，不能只看 origin：`https://grok.com/` 与 `https://grok.com/imagine` 同源，前者不含 AIGC、后者仅 AIGC。
- 键盘：↑↓ 切换、Enter 或 Tab 插入、Esc 关闭并保留已输入的 `//` 文本。鼠标点击结果同样插入。
- 插入时删除用户输入的 `//` 与搜索词，把资产的完整内容写入光标处；Skill 插入完整 `SKILL.md` 原文。插入后光标落在内容末尾，不自动发送。
- 若输入框不可写（如富文本编辑器拒绝程序化写入），退化为复制到剪贴板并在面板位置显示“已复制，请粘贴”。
- 面板为空库或无匹配时显示“没有匹配的资产”，并提示可在弹窗中新建；不预置示例。
- 每次成功插入记为一次取用。

### 视觉

- 面板延续浅色资料库视觉：暖白背景、极浅灰边框、雾蓝选中态；宽度跟随输入框但不小于 360 px、不大于 560 px。
- 使用 Shadow DOM 隔离样式，不影响宿主页面，也不被宿主页面样式污染。
- 不展示 Logo 以外的品牌元素，不加动画以外的装饰。

## 就地保存

- 扩展注册一个右键菜单项，仅在存在选中文字时显示：“保存到 FutureContext”。
- 点击后立即以选中文字为正文创建一条通用 Prompt：无标题、未分类、`titleSource` 与 `categorySource` 为 `none`；若后台 AI 已开启则按二期规则进入队列。
- 保存成功后，工具栏图标显示 3 秒的“✓”徽章；若当前站点为启用站点，同时在页面右下角显示轻提示“已保存到 FutureContext”，2 秒后消失。
- 选中文字为空白或超过 100,000 字符时不保存，徽章显示“!”并在弹窗顶部显示原因。
- 就地保存不弹出编辑页、不询问分类；用户之后在弹窗中修正即可。
- 右键菜单不提供保存为 Skill 或 AIGC，也不提供“保存整段对话”。

## 取用次数、常用排序与置顶

### 数据

- 每条 AI 资产新增 `useCount`（默认 0）、`lastUsedAt`（默认 null）、`pinned`（默认 false）。
- 显式复制（列表行或编辑页的复制按钮、Skill 详情页的复制）与就地取用成功时，`useCount + 1`，`lastUsedAt = now`。
- 复制与取用不改变 `updatedAt`，也不触发后台 AI 入队。
- 备份包含这三个字段；导入时若条目被判定为重复而跳过，不合并次数。重复判定继续使用现有指纹，不把这三个字段纳入指纹。
- 数据库新增 `usage.log`：最近 90 天的取用时间戳数组，用于设置页概览；超过 90 天的记录在写入时裁剪。

### 常用排序

- 通用 Prompt、Skill、普通 AIGC 与终端指令列表的工具区，在搜索框和 `全部分类 ▾` 之间加入 `最近编辑 ▾` 排序切换，可选：最近编辑、最近取用、最常取用。
- 选择按页签记住，存于 `settings.sortBy[tab]`；默认最近编辑。
- 最近取用：`lastUsedAt` 降序，从未取用的条目按 `updatedAt` 降序排在后面。最常取用：`useCount` 降序，相同则按 `lastUsedAt` 降序。
- 私密库列表不提供排序切换，仍按最近编辑。

### 置顶

- 列表行与编辑页提供“置顶 / 取消置顶”操作；列表行以细线图钉图标表示已置顶。
- 置顶条目在任一排序中始终位于列表顶部；置顶之间遵循当前排序规则。
- 取用面板同样置顶优先。
- 置顶是人工决定，后台 AI 不修改它。私密 AIGC 不提供置顶。

### 设置页概览

- 设置页新增“取用概览”：本周取用次数、近 30 天取用次数、累计取用次数、启用站点数量。
- 概览只读，用于让用户自己判断产品是否被用起来；不提供导出或分享。

## 权限

- `manifest.json` 新增 `contextMenus`。
- 就地取用所需的站点权限全部走 `optional_host_permissions`，仅在用户启用某站点时请求；停用时撤销。
- 快捷键通过 `commands` 声明，默认 `Alt+Shift+F`。
- 不新增 `tabs`、`webNavigation`、`clipboardRead` 或任何默认生效的主机权限。

## 数据与兼容性

- 数据库版本号保持为 2，不升级。三期只在资产上新增可选字段（`useCount`、`lastUsedAt`、`pinned`）、在 `settings` 下新增 `sortBy` 与 `inPlace`、在根上新增 `usage`；`normalizeDatabase` 读取时为缺失字段补默认值。这样回退到二期代码时，旧代码会原样保留这些未知字段而不会把库判定为空，用户数据不受影响。
- 备份格式版本同样保持为 2；新字段随资产写入备份，旧版本导入时会忽略它们。
- `normalizeDatabase` 遇到比当前代码更新的版本号时，不再返回空库：原样保留数据并标记为只读，弹窗顶部提示“数据来自更新版本的 FutureContext，请升级扩展”，所有写入操作（包括后台 AI 的状态写回）跳过。详见 ADR 0006。
- 模式演进规则（增量、不删不改名、版本号只在不兼容时升且附迁移）记录于 ADR 0006，后续各期遵守。
- 一期、二期所有既有交互、确认文案与私密库规则不变。

## 明确不做

- 单个 `/` 触发、在 ChatGPT 等站点自带斜杠菜单上叠加。
- 变量占位符（`[topic]`、`{{date}}`）、Prompt 链、插入后自动发送。
- 悬停 AI 回复的“保存本条消息”按钮、保存整段对话、自动识别页面上的 Prompt。
- 内容脚本读取或修改页面上除当前输入框之外的任何内容。
- 使用数据上报、云端排序、跨设备同步取用次数。
- 手动拖拽排序、按标签排序。

## 后续方向

- 变量占位符与插入前的快速填空。
- 在启用站点为用户自己发出的消息提供“保存本条”入口。
- 面板内直接新建一条通用 Prompt。
