# FutureContext 四期产品规格

## 目标

一期到三期的资产都是 Prompt 或 Skill。实际使用中还需要收集终端 / CLI 指令：AI Agent 的更新命令、免确认参数、`chrome://restart`、快捷键速查。这些不是 Prompt，不该塞进通用 Prompt，也不该进私密库或发给 LLM。

四期新增第四类资产「终端指令」，接入现有普通库框架：一级分类、搜索、复制、置顶、草稿、备份。不做预置内容。

## 范围与边界

- 仅存在于普通库，不能移入私密库。
- 不参与后台 AI 整理，永不发送给任何 Provider。
- 默认不出现在就地取用面板：聊天页仍只搜通用 Prompt 与 Skill，绘画页仍只搜普通 AIGC。
- 不抬数据库版本号：在既有资产结构上增加 `command` 类型与对应分类区域，旧备份可原样导入。

## 资产规则

| 资产 | 编辑内容 | 列表呈现 | 复制内容 |
| --- | --- | --- | --- |
| 终端指令 | 必填多行命令、分类；无标题 | 等宽命令正文前两行 + 分类标签 | 命令原文 |

- 命令本身即标识。多行命令可用 `#` 注释行自描述。
- 空库不预置任何指令。
- 删除、置顶、搜索、草稿、备份规则与其它普通库资产相同。
- 保存与分类变更都将用户选择记为人工分类。

## 页签

- 顶部页签顺序固定为：`通用 Prompt`、`Skill`、`AIGC Prompt`、`终端指令`。
- 720px 弹窗内四个页签保持同一字号与间距，不折行、不缩小、不加溢出菜单；`终端指令` 排在最后，因为它不是 Prompt / Skill。
- `终端指令` 激活态使用低饱和绿色，仅作用于该页签文字与下划线；其它页签仍用雾蓝。
