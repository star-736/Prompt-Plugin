# 绑定后对照磁盘，但不把外来同名 Skill 当成可撤回投递

界面不能只看库内 `skillDelivery`：用户自行放进 Cursor / Codex 的 Skill 没有 FutureContext 标记，却会被显示成“未投递”。绑定 DirectoryHandle 之后扫描该目录以反映磁盘实情；有本条目标记则记为已投递或库已更新并写回元数据，同名但无本条目标记则为“目录里已有”。不提供接管标记，以免随后撤回删掉用户自己的文件；未绑定目录时也不假装已扫描整个用户主目录。
